# mxpio-boot-example

MxpIO Boot 示例工程，演示如何基于框架快速构建 Spring Boot 应用。

## 启动

```bash
cd examples/mxpio-boot-example
# 修改 src/main/resources/application-dev.yml 中的数据库连接信息
mvn spring-boot:run
```

启动后访问 `http://localhost:9005/swagger-ui.html` 查看 API 文档。
