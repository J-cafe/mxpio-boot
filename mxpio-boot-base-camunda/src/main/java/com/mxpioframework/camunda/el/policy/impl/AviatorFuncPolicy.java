package com.mxpioframework.camunda.el.policy.impl;

import com.mxpioframework.camunda.el.policy.Context;
import com.mxpioframework.camunda.el.policy.FuncPolicy;
import org.springframework.stereotype.Component;

@Component("mxpio.camunda.aviatorFuncPolicy")
public class AviatorFuncPolicy implements FuncPolicy {
    @Override
    public void apply(Context context) {

    }
}
