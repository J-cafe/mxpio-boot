// import { useAssetsStore } from '@/store/modules/assets';

// // 注册项目内私有store
// function registerStore(storesInstance) {
//   storesInstance.registerStore('useAssetsStore', useAssetsStore);
// }

// export default registerStore;

// 自定义store示例，可以根据实际项目需要添加
export { useAssetsStore } from './modules/assets';
